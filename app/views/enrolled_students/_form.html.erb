<%= form_for(@enrolled_student) do |f| %>
  <% if @enrolled_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enrolled_student.errors.count, "error") %> prohibited this enrolled_student from being saved:</h2>

      <ul>
      <% @enrolled_student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
